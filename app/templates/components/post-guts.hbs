<li>
  <div class="">
    <iframe width="420" height="315"src="{{post.dustinSong}}"></iframe>
  </div>


  {{#each post.images as |image|}}
  <img src={{image}} alt="{{post.title}}" />
  {{/each}}

<h3>{{post.title}}</h3>
</li>
